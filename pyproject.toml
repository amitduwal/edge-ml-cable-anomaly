[project]
name = "thesis-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "h5py>=3.16.0",
    "matplotlib>=3.10.8",
]

[tool.uv.workspace]
members = []

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
